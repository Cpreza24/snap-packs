<nav>
    <% if(user) { %>
        <a href="/users/<%= user._id %>/home">Home</a>
    <a href="/users/<%= user._id %>/camera">Camera Gear</a>
    <a href="/users/<%= user._id %>/lens">Lens Gear</a>
    <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
    <a href="/auth/sign-in">Sign In</a>
    <a href="/auth/sign-up">Sign Up</a>
    <% } %>
</nav>